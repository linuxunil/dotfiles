---
# Development tools for all platforms - override in vars/
dev_packages: []

# Platform-specific packages
dev_packages_macos:
  - podman-desktop

dev_packages_linux:
  - podman-compose
  - buildah
  - skopeo

# Language configuration - now managed by mise
languages: []  # Defined per machine

# Mise language configuration
mise_languages: []  # Defined per machine, format: [{name: "go", version: "latest"}, ...]